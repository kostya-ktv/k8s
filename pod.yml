apiVersion: v1
kind: Pod
metadata:
  name: short-service
  labels:
    components: frontend
spec:
  containers:
    - name: short-service
      image: antonlarichev/short-app
      ports:
        - containerPort: 80
      resources:
        limits:
          memory: "128Mi"
          cpu: "500m"